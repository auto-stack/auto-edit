# L0 Proxy 基线（首跑）— 2026-09-21

> **口径**：PLAN-005 T-07 首跑摘要。L0 = VM+merged（**无绝对性能效力**，
> 战略 §5 阶梯）——本表价值 = 启动链**形状**分解 + 结构回归代理基线 +
> 缓冲区类记账基线（rope 前禁调优，战略补注 6(a)）。数字永不入公开
> 对比面。工具链 `v0.4.2-1652-g055808724-dirty`（debug 构建）；本机
> 多会话并行（数字仅供形状参考）。数据源 `results/20260921-143138.jsonl`。

## 启动链分解（5 跑，首跑弃暖机）

| 跑次 | spawn→vm_init (ms) | vm_init→ws_loaded (ms) | 空闲内存 (MB) |
|---|---|---|---|
| run1 | 534.1 | <0.1 | 87.7 |
| run2 | 315.2 | <0.1 | 88.4 |
| run3 | 286.8 | <0.1 | 87.5 |
| run4 | 312.7 | <0.1 | 88.1 |
| **均值** | **362.2** | **<0.1** | **87.9** |

形状结论：启动链成本几乎全部在 **spawn→vm_init**（auto.exe 进程引导 +
VM 装载，~0.3–0.5s）；`vm_init→ws_loaded`（目录树装载）低于 25ms 轮询
粒度——workspace 装载不是当前形状瓶颈。内存方法：
`powershell.Get-Process.WorkingSet64`（psutil 缺席回退，方法已记入 JSONL）。

## 打开文件计时（ConsumeOpen 链，host 时间戳包夹）

| 尺寸 | open (ms) | 装载后内存 (MB) | fixture 生成 (s) |
|---|---|---|---|
| 1 MB | 25.7 | 229.5 | 0.001 |
| 10 MB | 25.7 | 251.3 | 0.006 |
| 100 MB | 459.8 | 521.1 | 0.123 |

形状结论：1–10 MB 在同一量级（~26ms）；100 MB 打开 ~0.46s 且**内存随
文件线性放大**（~88 → ~521 MB）——整串镜像在位的直接证据，即预算表
`open_100mb`/`open_1gb`/`idle_mem` 三行 arch-blocked/ledger 记账基线
（rope 化后的前后对照锚点）。注意本预算行不判绿红（禁调优，只记基线）。

## 结构回归代理

- **编辑路径全量读检测：green**——`code_editor_text` 全 front 面仅存
  editor_store 的 ActSave/QuitSaveClose 两个 save 上下文（A 段去镜像的
  首个绿证）；构造性红证自检 PASS（bench check 内置样例）。

## 预算断言终态（全 10 行，五类齐）

| 状态 | 行 |
|---|---|
| not-armed（硬门禁非 L2） | steady_start / renderer_cold_start |
| pending-feature | warm_start（M2）/ installer（Q2） |
| arch-blocked（只记基线） | open_100mb / open_1gb |
| blocked-upstream | type_latency / scroll_fps（内核帧插桩）、diff_100mb（M3） |
| ledger（记账不阻塞） | idle_mem |

`--mode l1/l2` 构造性验证：均经 perf.py 链归因 **exit 3**（§6 RQ 渲染臂
覆盖 / §7 a2r 词汇门 PLAN-027——与主检出基线对照同因同形）。
